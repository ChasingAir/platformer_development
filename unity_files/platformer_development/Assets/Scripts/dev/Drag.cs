//This is the script for dragging objects. This needs to be written in such a way that it can be applied to any game object

using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Drag : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
      //if(touching screen)
      //{
        //if(touching THIS_OBJECT)
        //{
          //move object same amount that finger moves
        //}
      //}
    }
}
